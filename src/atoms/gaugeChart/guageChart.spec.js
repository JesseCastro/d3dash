describe('GuageChart Atom Module', () => {
  it('should exist', () => {
    expect(D3Dash.GuageChart).not.to.eql(undefined);
  });
  it('should return foo', () => {
    expect(D3Dash.GuageChart.foo()).to.equal('foo');
  });

});
